# config/local.yaml — development environment (default when UNM_ENV unset)
# Only overrides what differs from base.yaml.

ai:
  reasoning:
    advisor: "medium"
    advisor_deep: "high"
    whatif: "high"

features:
  debug_routes: true
